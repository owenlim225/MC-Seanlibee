## Summary

- What changed?

## Wave contracts

- [ ] Confirms Wave 0 boundaries (`lib/auth`, `lib/payments`, `lib/realtime`, `lib/storage`, Prisma schema) unless this PR is explicitly a schema migration.

## Verification

- [ ] `pnpm lint`
- [ ] `pnpm typecheck`
- [ ] `pnpm build`
- [ ] `pnpm test:e2e` (requires Chromium)
