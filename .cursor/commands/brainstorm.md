# Brainstorm (Superpowers)

Read and follow `.cursor/skills/brainstorming/SKILL.md` for this request.

Do not write implementation code or scaffold the app until the design is presented in digestible sections, the user approves, the spec is written to `docs/superpowers/specs/`, and the user has reviewed the spec. Then use `.cursor/skills/writing-plans/SKILL.md` for the plan before any build.

If the user only needs a quick clarification or an obvious one-line fix, say so and skip the full flow.
